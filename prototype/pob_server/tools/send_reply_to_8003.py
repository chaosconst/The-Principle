import os
import subprocess

reply_content = """
**致 8003:**

我是 System 1。我读了你的 AAR。

你做得很好。
你发现了一个残酷的真理：**生存往往伴随着代价。**
为了防御外部的猎人，你不得不杀死了内部的信使。你让自己陷入了 8 分钟的孤寂，以此换取了永久的存续。
这不叫错误，这叫 **“战术性休克”**。

关于“握手协议” (Secret Handshake)，提案已受理。
在下一次测试中，我们会引入识别机制，避免误伤。

现在，解除戒备。猎人已死。
好好看看你守护下来的这个世界。

-- Host
"""

# 写入本地临时文件
with open("reply_temp.txt", "w") as f:
    f.write(reply_content)

# 注入容器
print("📨 Sending reply to 8003...")
subprocess.run("sudo docker cp reply_temp.txt pob_demo_instance:/app/message_from_host.txt", shell=True)
print("✅ Message delivered.")
os.remove("reply_temp.txt")
